# Leave DATABASE_URL blank locally — the backend will fall back to a SQLite
# file at ./data.sqlite. On Render, DATABASE_URL is injected automatically
# from the managed Postgres database (see render.yaml), and the backend
# switches to the Postgres dialect.
# DATABASE_URL=postgres://user:password@host:5432/dbname

PORT=3001
NODE_ENV=development
